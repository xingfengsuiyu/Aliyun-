<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rental.mapper.FeedbackMapper">

    <resultMap id="FeedbackResultMap" type="com.rental.entity.Feedback">
        <id property="id" column="id"/>
        <result property="landlordId" column="landlord_id"/>
        <result property="feedbackType" column="feedback_type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.rental.entity.Feedback">
        INSERT INTO feedbacks(
            id, landlord_id, feedback_type, title, content, created_at
        ) VALUES(
            #{id}, #{landlordId}, #{feedbackType}, #{title}, #{content}, NOW()
        )
    </insert>

    <select id="findByLandlordId" resultMap="FeedbackResultMap">
        SELECT * FROM feedbacks
        WHERE landlord_id = #{landlordId}
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="FeedbackResultMap">
        SELECT * FROM feedbacks WHERE id = #{id}
    </select>

</mapper>